package T00_MoreExercise;

import java.util.Scanner;

public class M02_AsciiSumator {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        String firstChar = scanner.nextLine();
        String secondChar = scanner.nextLine();
        String containsChars = scanner.nextLine();

         int first = firstChar.indexOf(0);
         int second = secondChar.indexOf(0);
         int sum = 0;


    }
}
